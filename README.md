# ntail
Network tail - sniffer for syslog messages.

```
➜  ~  ./ntail -h
Usage of ./ntail:
  -d string
    	device (default "eth0")
  -p string
    	port (default "514")
  -prot string
    	protocol (default "udp")
```
